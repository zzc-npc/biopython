# 项目说明
用于练习numpy，pandas，Matplotlib
# 环境说明
## 激活conda环境
conda activate rna_project
## 核心功能
NumPy：数组创建、索引切片、广播、矩阵运算、随机数、统计计算。
pandas：读取 CSV/Excel、筛选、排序、缺失值处理、分组聚合、合并、时间序列。
Matplotlib：折线图、散点图、柱状图、直方图、子图、图例、保存图片。
Jupyter：写笔记、分块运行代码、即时可视化、做小实验。
Biopython：如果当时做 RNA 序列相关练习，可读写 FASTA/FASTQ、翻译序列、解析比对结果等。
# 文件结构
biopython/
├── pyproject.toml
├── README.md
├── .gitignore
├── data/
│   ├── raw/                 # 原始数据，只读
│   │   └── rfam_fasta/
│   └── test/                # 小测试文件
│       ├── test.fa
│       └── test.fa.gz
├── results/                 # 输出
│   └── figures/
├── src/
│   └── rna_toolkit/
│       ├── __init__.py      # 包导出
│       ├── __main__.py      # 瘦入口，只调 cli.main
│       ├── cli.py           # build_parser + main
│       ├── fasta.py         # 解析器
│       └── commands/        # 子命令
│           ├── __init__.py
│           └── stats.py     # cmd_stats + register
├── tests/
│   └── test_fasta.py
└── notebooks/               # 学习笔记
    ├── numpy/
    └── python_base/