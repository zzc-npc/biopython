# 此项目用于练习python基础
# 1. 手写 FASTA 解析器 
## 工具内容：
### 工具模块：
1. gzip 用于识别gz压缩格式
2. pathlib的Path，*暂时没搞懂*
3. typing中的Iterator 生成器yield
## 版本迭代
*版本一*：使用retuen，将fa文件返回为一个list[str(header),str(sequence)]，过于占内存
*版本二*：改用yield流式生成，不占内存。
*版本三*：导入gzip模块，用于识别gz格式文件，因为我们网上之间下载下来的rna文件多是这个格式
## 工具的使用
### 1.导入read_fasta工具 = 添加路径+导入
 
import sys
sys.path.append("/home/zzc/projects/biopython/src")

from fasta import read_fasta
### for循环使用
使用for，自动next（），流式输出，不保存
### list
直接所有保存进list，但是就丢失了yield的优势了

# 哪些内容不要手写
argparse 配置
pyproject.toml
__init__.py
json.dump 参数
Path 路径拼接
CLI 分发
# 哪些要手写
read_fasta 状态机
N50 算法
中位数
k-mer 滑窗
GC 含量
倒排索引
搜索算法